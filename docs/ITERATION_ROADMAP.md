# 迭代路线图

> **本文档是项目的执行顺序与演进日志**，按"能跑的增量"组织，而非按模块组织。
>
> 配套：[`BADCASES.md`](BADCASES.md)（缺陷记录与修复过程）、[`MIGRATION_PLAN.md`](MIGRATION_PLAN.md)（文件级改动清单）、[`DESIGN_CORE_MECHANISMS.md`](DESIGN_CORE_MECHANISMS.md)（核心机制设计）

## 使用约定（重要）

每轮迭代包含「预期问题」与「实际发现」两栏：

- **迭代 0 已完成**，其中的发现是**真实实测结果**，有数据和提交记录支撑
- **迭代 1 及以后是计划**，「预期问题」是推演，**不得当作已发生的事实对外描述**
- 每轮做完后**回填「实际发现」**，写实际遇到的问题——包括与预期不符的部分。预期错了本身就是有价值的记录

> 这份文档做完就是项目的真实演进史，可直接支撑 README 与面试问答。**它的价值恰恰在于诚实**：编造的演进史经不起追问，真实的演进史每个转折都有据可查。

---

## 演进主线

```
v0.1 能跑通一次        →  发现输出不可控
v0.2 定义工作流        →  发现模型会编造未知字段
v0.3 反幻觉闸门        →  发现无法判断改进是否有效
v0.4 让它可评测        →  发现缺数据时系统给低风险（危险）
v0.5 风险评分+完整度闸门 →  发现高风险不该全自动放行
v0.6 人机协同          →  倒逼恢复 LangGraph 真实执行
v0.7 性能与成本        →  发现 834s/次不可接受
v1.0 交付形态
```

每个箭头都是一个真实的因果链。这条链本身就是项目叙事。

---

## 迭代 0：接手与基线 ✅ 已完成

**目标**：搞清楚接手的是什么，能不能跑，问题在哪。

**做了**
- 建专属环境，起 6 个容器，跑通前后端（提交 `e11ea53`）
- 同一问题跑 V1(ReAct) 与 V2(多智能体) 做对照实测
- 通读架构，产出 `DATA_STRUCTURES.md` 阅读路径与 `LEARNING_GUIDE.md`

**实际发现**（真实数据，见 [`V1_VS_V2_BASELINE.md`](V1_VS_V2_BASELINE.md)）

| 发现 | 证据 |
|---|---|
| V1 把源文档占位符当数据写入报告并挂引用角标 | `2019年交易规模达 XX万亿元 ##1$$` |
| V1 反思能识别缺口但生成空查询，回退后命中缓存空转 | 后端日志 `additional_queries: [{}]` |
| V2 收益真实但代价高 | 74s→834s，信源 5→90，报告 1913→8380 字 |
| V2 的 Critic 精确点名了让 V1 栽跟头的信源 | `[critical] hallucination: "原创力文档"非权威机构` |
| **即便 V2 自评也只有 5/10、4 个 critical** | `verdict=needs_revision` |
| Critic 被要求判断时效性却从不注入当前日期 | 把一年前的 2025年4月 判为"未来事件" |
| LangGraph 编排是死代码，实际走手写编排 | `graph.py:349-356` 整段注释 |

**结论**：问题不在"行业选得不对"，而在**输出不可信**。换个行业解决不了任何事。真正值得做的是：在一个不允许幻觉的场景里，把不可信输出治理成可信输出。这直接决定了后续所有迭代的方向。

---

## 迭代 v0.1：最小闭环 Demo

**目标**：端到端跑通一次企业尽调，**哪怕很粗糙**。先证明路走得通。

**动机**：迭代 0 只验证了原系统能跑行业研究。尽调是不同的任务形态（对象是单一主体、有固定必查项、要求可溯源），得先确认现有 Agent 骨架扛不扛得住。

**范围内**
- 硬编码 1-2 家虚构企业的完整档案（JSON 文件即可，**不建数据库表、不做适配层**）
- 改 Architect / Writer 的提示词，让大纲和报告是尽调形态而非行业研报
- 复用现有 SSE 与前端，能看到过程和最终报告

**明确不做**：数据库建模、数据源适配层、评分卡、评测、前端改造、LangGraph 修复

**预期问题**
- 报告结构每次都不一样，无法横向比较
- 模型会给硬编码数据里**没有的字段**自行编值
- 无法回答"这份报告核实了多少项、漏了多少项"

**验收**：`/research/stream` 传一个企业名，能流式产出一份读起来像尽调报告的东西。

> 关键：这一轮的目的**不是做好，而是快速暴露问题**。做得越糙、问题暴露得越早越好。

### ✅ 实际发现（2026-08-10 回填）

两次运行：run1 网络搜索意外开启（因 BC-05），run2 为纯内部数据源的干净对照。

| | run1（含网络） | run2（纯档案） |
|---|---|---|
| 耗时 | 1076 s | 1086 s |
| 事实数 | 129（仅 15 条来自档案） | 31 |
| 报告字数 | 10,530 | 8,332 |
| 质量分 | 2/10 | 异常值 -1 |
| critical | 4 | 4 |

**① 预判错误：模型没有编造。** ⭐

预判"模型会给未提供的字段自行编值"，**没有发生**。第 6 章（关联关系与对外担保）
在 0 条档案事实下写了近千字，全部是规范的未核实声明：

> 实际控制人未核实（原因：工商登记数据未包含实际控制人认定信息）
> 对外担保……均未核实（原因：数据源未提供）

干净运行的量化结果：`未核实` 出现 **30 次**，"无对外担保/未发现担保"这类
错误断言 **0 处**，占位符 **0 处**。它还主动论证了信息缺口本身构成的风险。

**这个预判失败改变了 v0.2/v0.3 的动机表述**：不再是"模型会编造，所以要加约束"，
而是——**软约束这次生效了，但我无法回答"生效率是多少"**。
一次成功不等于每次成功，而目前没有任何手段度量。
这比原来的理由更直接地指向 v0.4 的评测能力。

**② 意外发现：虚构主体与真实检索不可调和**（BC-10）

run1 中 129 条事实里 114 条来自网络。搜索虚构公司名返回的是**其它真实同名企业**，
"3家疑似关联企业""企业状态：注销"被错误归属给尽调对象，与档案的"存续"直接冲突。
Critic 正确识别为"主体真实性幻觉"并拒绝下结论——处理是对的，但整份报告不可用。

**直接推翻了 v0.4 的一个设计假设**：原计划"mock 适配层 + 网络搜索作补充"不可行，
**评测链路必须是纯 mock 闭环**。这是靠实际运行才发现的约束，纸面推不出来。

**③ 审核者自身不可靠会污染全局**（BC-03 复发加重）

run2 的 4 个 critical 中 **3 个源自同一个 bug**：Critic 把 2025 年财报和
2026 年 3 月的被执行记录判为"尚未发生的未来事件"。更棘手的是，
它在同一句里自己写出了"报告撰写于2026年8月9日"——**日期就在上下文里，
训练先验仍然压过了它**。已在 `BaseAgent` 统一注入时间基准并附判定规则修复。

**④ 顺带修掉 4 个既有缺陷**：BC-05（GET 端点静默丢参数）、
BC-09（检查点保存 100% 失败）、BC-11（Scout 拒绝纯内部数据源模式）、
以及无效检索词过滤（模型产出过"（此信息无法通过公开搜索获得）"这样的"检索词"，
系统照单全收拿去搜索）。

**⑤ 待处理的观察**
- Scout 每轮只检索 `pending_sections[:3]`，而 Writer 写全部 8 章
- Scout 生成的检索词常是整句散文，已加形态过滤，但根治要靠 v0.2 的清单化检索式
- `quality_score` 出现 -1 异常值，需查解析逻辑

---

## 迭代 v0.2：定义工作流（清单驱动）

**动机**（据 v0.1 实测结果修正）：原假设是"模型会编造未知字段"，
实测**没有发生**——提示词软约束这次生效了。但真正的问题浮现出来：

> **软约束是否稳定生效，我无法回答；因为我没有任何手段度量它。**

一次运行的成功不能推广。而且当前"未核实"散落在自由文本里，
既不能统计核实率，也不能让下游（评分、复核、评测）消费。
所以 v0.2 的目标从"防止编造"调整为——
**把隐式的、只存在于文字里的核查状态，变成显式的、可计数的结构化数据。**

这同时是 v0.4 评测的前置条件：没有结构化的 `field_checks`，
"未核实识别率"这个指标根本无从计算。

**核心改动**：把自由生成翻转为清单驱动（设计见 `DESIGN_CORE_MECHANISMS.md` 第一部分）

```
v0.1：搜索 → 提取 → 写报告            （缺失是静默的）
v0.2：生成必查清单 → 逐项核查 → 写报告  （缺失是显式的）
```

**范围内**
- `state.py` 增 `FieldCheck` TypedDict 与 `field_checks` 字段
- Architect 产出 20 项核查清单骨架（14 必查 + 6 选查）
- Scout 改为逐项取证，填充 status/value/sources
- Writer 按清单逐项渲染

**明确不做**：unverified 的强制约束（下一轮）、评分、评测

**预期问题**
- 模型仍会把取不到的字段填上"合理"的值，因为 prompt 只是"建议"它标未核实
- 清单填充率低，部分字段无从下手
- Writer 可能悄悄跳过状态不佳的字段

**验收**：每次尽调都产出结构一致的 20 项清单，能统计核实率。

### ✅ 实际发现（2026-08-10 回填）

**验收通过。** 首次得到核实率：**11/15 = 73%**（耗时 630s，报告 7558 字）

| 维度 | 核实 | | 维度 | 核实 |
|---|---|---|---|---|
| basic | 3/3 | | judicial | 4/4 |
| financial | 4/4 | | opinion | 1/2 |
| equity | 1/3 | | operation | 1/1 |
| relation | **0/3** | | | |

反幻觉指标：`未核实` 23 处、`经查询无记录` 3 处、
**把担保缺口误写成"无担保"的危险表述 0 处**。

**① 设计漏洞：「查了但无记录」≠「未查询」** ⭐

第一版跑出 10/15 时，"失信记录"被判为未核实。但档案里没有失信条目有两种可能：
查了司法源返回空（**已核实的正面结论，可支持授信**）vs 根本没查（信息缺口）。
原实现只看"字段有没有"，把两者混为一谈。

在尽调中这是严重错误：把有利结论误算成缺口还算保守，
**反过来把缺口当成"无记录"就是直接误导审批**。

改为让数据源显式声明 `coverage.queried` 覆盖范围。修正后 11/15，
失信记录/股权冻结/对外投资正确归类为"经查询无记录"。
**纸面设计没想到，写代码时才撞出来。**

**② 时间基准修复确认生效，但汇总字段不可信**

Critic 详细条目明确写道"根据时间基准，2026年3月14日是过去日期"——
对比 v0.1 的"引用了尚未发生的未来事件（2025年4月）"，推理已纠正。

但它的 `summary` 字段仍写"多处将未来事件作为已核实事实"，
**与自己的逐条结论自相矛盾**。教训：LLM 产出的汇总字段比逐条结论更不可靠，
下游消费应以结构化条目为准，不要用 summary 做判断。

**③ Critic 提出了一条真正有价值的批评**

> 报告一方面指出2025年数据为"企业自报（未经审计）"可信度较低，
> 另一方面却将其与2023、2024年的审计数据并列做趋势分析并得出确定性结论，
> 这存在严重逻辑缺陷，具有误导性。

完全正确。已把这条要求写进 Writer 提示词：跨年度趋势分析若各期证据等级不同，
必须显式指出并说明不确定性。**审核 Agent 的有效批评应当反哺提示词。**

**④ 顺带修掉两个输出缺陷**
- `quality_score` 实测返回 `-1`，而提示词声明范围是 1-10，且无任何校验。
  已加类型与范围裁剪——下游阈值判断、评测、前端都依赖这个范围
- 内部字段泄漏：模型把 `可信度: 0.95` 原样抄进报告正文。
  改为语义化证据等级（官方登记信息／经审计资料／企业自报未经审计／公开报道需佐证）

**⑤ 遗留**
- mock 数据的 `retrieved_at` 全为同一天，Critic 认为"不可能今天核查了所有信息"。
  真实数据源的获取时间应有差异，v0.4 适配层实现时处理
- Writer 会自行扩展清单外的核查项（如"法定代表人个人涉诉"）并标未核实。
  行为正确，但这些项不在 `field_checks` 统计内，核实率与报告内容存在口径差

---

## 迭代 v0.3：反幻觉闸门

**动机**（据 v0.1/v0.2 实测修正）：原假设"模型倾向于编造未知字段"**两轮实测均未出现**——
v0.2 干净运行中未核实表述 23 处、危险断言 0 处。所以 v0.3 不是去堵一个没发生的问题。

真正暴露出来的是另一类风险：**判定逻辑本身会出错，而且错得很隐蔽。**
v0.2 代码评审抓到——把属性型字段的空查询结果解释成"经查询无相关记录"，
等于把"主体可能不存在"这个 critical 信号粉饰成中性结论；
主体归属未确认的舆情被计入已核实。**这些错误发生在模型之外，在我写的规则里。**

因此 v0.3 的目标调整为：**在报告产出侧建立独立校验，防止清单状态被错误消费**。
清单状态可能是错的，Writer 的表述也可能偏离状态——需要一个不信任上游的检查者。

**范围内**
- `unverified` / `conflicting` 作为一等状态，附 `failure_reason`、`attempted_sources`
- Writer 三种状态的**固定渲染模板**，禁止推断、禁止省略、禁止用"暂无"替代"未核实"
- Critic 新增三类检测：`unverified_as_fact` / `conflict_silently_resolved` / `unsupported_risk_conclusion`
- 顺手修迭代 0 发现的日期未注入缺陷（BaseAgent 统一注入当前日期）

**预期问题**
- Critic 会漏检，尤其是隐含断言（"该企业经营稳健"暗示了财务已核实）
- **无法量化改进幅度**——只能凭感觉说"好像好点了"

**验收**：人为构造一个"司法数据源返回空"的用例，报告必须写"涉诉记录未核实"，且绝不出现"无涉诉记录"。

> 这一轮结束时会撞上一堵墙：**改好没有？不知道。** 这就是下一轮的动机。

### ✅ 实际发现（2026-08-10 回填，v0.3 与 v0.4 合并执行）

因 v0.2 的 5 个缺陷有 3 个靠人工审阅发现而非跑出来（端到端数字 73% 看着很合理，
不足以暴露规则错误），决定**先建评测再改 Critic**，两轮合并。

**评测框架分两层**（分层依据：缺陷全部出在判定规则而非模型输出）

| 层 | 是否调 LLM | 耗时 | 结果 |
|---|---|---|---|
| 快速层 `run_fast.py` | 否 | 秒级 | 字段状态 86/86、无记录识别 18/18、主体异常 4/4 全部 100% |
| 校验层 `run_critic.py` | 是 | 约 8 分钟 | 见下 |

**检出侧完全达标**：10 个注入用例 × 3 次 = 30 次运行，**稳定检出 10/10，零波动**。
含最隐蔽的形态：隐含断言（"资信良好、经营合规"）、把未核实写成"不存在"、
冲突时采信另一方、用"暂无"替代"未核实"。

**误报侧不达标**，且暴露了三个方法论问题——**这一轮真正的收获在这里**：

1. **BC-12：评测装置不公平会产出误导性指标**
   首测误报率 100%，排查发现主因是对照文本只有三句话，Critic 却按完整报告审。
   若照此改 Critic，会把本来正确的检查器改坏。修正装置后 100% → 33%。

2. **BC-13：小样本调提示词是在拟合噪声**
   3 个对照用例上出现"改完失败用例换人而总数不变"。据此**主动停止调优**，
   把样本扩到 28 例（10 注入 + 18 对照）并引入**每例重复 3 次的方差分离**。
   分离后首轮：稳定通过 6/18、不稳定 7/18、稳定失败 5/18。
   单次口径会显示"误报率 51.9%"——**7 个方差会被误读成缺陷**，
   这正是扩样本要买的东西。

3. **BC-14：提示词补丁失效，加规则反而更差**
   针对稳定失败加了一段判定前提，结果三项指标全部恶化
   （稳定通过 6→3，稳定失败 5→7），且目标用例根本没修好。
   根因是我又一次先改被测对象——真正的问题仍在装置：
   harness 给单章节文本却配完整 20 项清单。**与 BC-12 同类错误的第二次出现。**
   已回退补丁，改为让清单范围与被审文本匹配。

**这一轮的核心产出不是某个百分比，而是一套能区分"真缺陷 / 方差 / 装置问题"的测量能力。**
在此之前，任何"改进了"的判断都没有依据。

---

## 迭代 v0.4：让它可评测 ⭐

**动机**：v0.3 做完无法回答"幻觉率降了多少"。**不能度量就不能改进**——这是整个项目的转折点。

**关键洞察**：要有 ground truth，就必须自己掌控数据。这才是**数据源适配层的真正动机**——不是"因为买不起企查查"，而是**"因为评测需要已知真值"**。

**范围内**
- `service/datasource/` 适配层：抽象基类 + mock 实现（工商/司法/招投标/舆情）
- Stage 1 数据模型落库（`Company`/`JudicialRecord`/`FinancialRecord`/...）
- `scripts/seed_companies.py` 生成 30-50 家虚构企业 + **ground truth 标注**
  - 覆盖优质/一般/高风险/担保圈四类画像
  - **刻意让部分字段"数据源查不到"**，用于测未核实识别
- 评测脚本，四项指标：字段准确率 / **未核实识别率** / 幻觉率 / 等级一致性

**预期问题**
- 首次跑分会很难看，尤其未核实识别率
- 会发现某些字段模型系统性地搞错
- **大概率会发现：数据缺失时系统仍给出乐观结论**

**验收**：`python -m scripts.eval` 输出四项指标表格，可复现。

> 副产品：mock 适配层让任何人 clone 下来无需付费 Key 就能跑通全流程。这解决了作品集项目"面试官跑不起来"的经典问题——但它是评测需求的副产品，不是目的。

---

### ✅ 补充：两轮外部代码评审（2026-08-10/11）

评审累计指出 13 处问题，**逐条对照代码核实后全部属实**，其中三条推翻了已发布结论：

| 被推翻的结论 | 实情 |
|---|---|
| "扫描器贡献 +6.7 个百分点" | `--ablate scanner` 只绕过结果合并，`_review_content()` 仍执行扫描并把结论写入提示词——消融组的 LLM 依然看得到。**该数字无效，已撤回** |
| "提示词禁止无效，代码兜底才有效（4/18→9/18）" | 评测调 `_review_content()`，而代码过滤在 `process()` 里，**评测从未走过那条路径**。改善只可能来自提示词。**归因错误** |
| "架构与模型完全正交" | 28 例已反复用于调优，存在开发集过拟合，只支持趋势性表述。**已收敛措辞** |

修正实验设计后重做的可比数据（同口径、同模型、同温度）：

| 配置 | 逐次检出率 | 漏检率 | 逐次无误报率 | 误报率 |
|---|---|---|---|---|
| A 完整架构 | **100%** | **0%** | 94.4% | 5.6% |
| B 真正关闭扫描器 | 96.7% | 3.3% | 92.5% | 7.5% |

**扫描器真实贡献：漏检率 3.3% → 0%，检出侧方差归零。**
数值不大但性质关键——尽调场景中"3.3% 的把未核实写成无记录会漏过去"
与"一次都不会漏"是两种产品。**确定性组件的意义不在提升平均分，
而在消除尾部风险。**

同时更正指标口径：此前所报"误报率 98.1%"实为**逐次无误报率**，
且只统计三种 issue 类型；按全口径为 94.4%（误报率 5.6%）。

**这两轮评审最有价值的产出是 BC-17**——把所有问题归纳出统一形态：
**实现了机制，但没有验证机制真正生效**。详见 [`BADCASES.md`](BADCASES.md)。

---

## 迭代 v0.5：风险评分与完整度闸门

**动机**：v0.4 的评测**预计会量化暴露最危险的失效模式**——司法数据源查不到 → 三项必查无扣分 → 综合分低 → 输出"低风险，建议授信"。

这是风控上不可接受的："查不到"被当成了"没问题"。

**范围内**（设计见 `DESIGN_CORE_MECHANISMS.md` 第二部分）
- `service/risk_scorecard.py`：五维规则评分，**纯函数、无 LLM、可单测**
- 完整度闸门：核实率 <60% 判"数据不足，无法评级"；司法维度缺项则等级至少中风险；存在冲突项则上调一级
- `triggered_rules` 记录每条规则的触发原因与证据 fact_id

**为什么规则不用 LLM**：评审会要问"为什么是高风险"，答案必须是"资产负债率 82% 超阈值 70%，且有 3 笔被执行"，而不是"模型认为"。且规则可单测、可复现，才能当评测基准。

**预期问题**
- 阈值需要反复调，容易过严或过松
- 闸门可能过于保守，大量案例落到"数据不足"

**验收**：评测中"数据缺失→低风险"的错误清零；每个等级结论都能追溯到规则与证据。

---

### ✅ 实际发现（2026-08-11 回填）

**闸门确实拦住了那个最危险的失效模式。** EVAL-003（司法数据源超时）：

```
综合分 9.0  →  本该判"低风险，建议授信"
闸门介入   →  中风险（司法维度必查项未核实）
```

五家评测企业的判定：

| 企业 | 核实率 | 综合分 | 等级 | 决定因素 |
|---|---|---|---|---|
| EVAL-001 优质 | 14/15 | 0.0 | 中风险 | relation 维度缺口 |
| EVAL-002 高风险 | 13/15 | 96.4 | **拒绝** | 失信一票否决 |
| EVAL-003 司法缺 | 9/15 | 9.0 | **中风险** | ← 闸门提升 |
| EVAL-004 主体疑 | 4/15 | 0.0 | **数据不足** | 核实率 27% |
| EVAL-005 冲突 | 12/15 | 15.0 | **高风险** | 冲突上调一级 |

**除 EVAL-002 外，其余四家的等级全部由闸门而非分数决定**——
这说明闸门不是补充规则，而是主要判据。

**① 综合分会被"表现好"的维度稀释**（测试中已锁定）

失信 + 2笔被执行 + 负债率89% + 严重舆情 + 连续亏损的企业，综合分仅 **47.5**：

```
financial  70.0 × 0.30 = 21.0
judicial   66.7 × 0.30 = 20.0
relation    0.0 × 0.20 =  0.0   ← 无担保，"好"维度稀释
operation  15.0 × 0.10 =  1.5
opinion    50.0 × 0.10 =  5.0
                          47.5   → 单看分数是"中风险"
```

这是加权平均的固有行为，**也正是闸门必需的原因**——真正挡住它的是
"失信 → 至少高风险"这条硬规则。**推论：综合分不可单独使用。**

**② 「低风险」等级当前不可达**（BC-18）

`guarantee_circle` 是必查项但**没有任何数据源**（需关联图谱推导，属 v0.6），
恒为 unverified → relation 维度恒被排除 → 等级下限恒被提升。
一个永远达不到的等级说明规则有问题。**未调阈值掩盖**，已写成显式测试，
待 v0.6 有图谱能力后决策。

**③ 接入主流程（2026-08-11 完成）**

评分卡已接进 DataAnalyst → SSE → Writer → `research_complete`。
设计与断言清单见 [`DESIGN_CORE_MECHANISMS.md`](DESIGN_CORE_MECHANISMS.md) 第四节。

接入过程中的三点实际发现：

**（a）最危险的失效面在接入处，不在评分卡里。**
`score()` 需要"清单状态 + 结构化档案"两份输入。清单说 `verified`
但档案没进 state 时，实测会输出**低风险**——
因为空的 `judicial_records` 被如实读成"未发现失信被执行人记录"。
这是纯粹由链路缺陷凭空造出的正面结论，且完整度闸门查不出来（清单确实是 verified）。
评分卡自身的规则单测一条都覆盖不到这个方向。
已按 fail-closed 处理，记为 [BC-19](BADCASES.md)。

**（b）行为断言又一次纠正了自己。**
"LLM 全挂时评级仍产出"这条用例最初是绿的，但其实 **LLM 根本没被调用**
——用例里 `facts` 为空，DataAnalyst 三个步骤都提前返回了。
靠一句 `assert raised` 的前提断言才发现。与 BC-15 同形：
跑通了，但测的不是声称的那件事。**前提断言应当和结论断言一样多写。**

**（c）机制之间会互相影响。**
评级块进入 `final_report`，而 Critic 扫描 `final_report`——
若渲染文本里出现"未发现 XX 记录"而该字段并非 verified，
系统就用自己的输出制造了一条 critical。实测 5 家评测企业均为 0 误报，
但那是"实现正确所以碰巧安全"，已锁为回归测试。

走生产路径（`DataAnalyst.assess_risk`）复跑 5 家评测企业，等级与上表逐一一致
（中风险 / 拒绝 / 中风险 / 数据不足 / 高风险），且每家都推出了 SSE 事件、
终局事件都带评级——接入未改变判定，只是让它可见、可推送、可进报告。

**④ 完成后独立复核（2026-08-11）**

发现并修复四条“机制存在但未完全生效”的链路缺陷：

- 维度完整缺失时因没有 `dim_scores` 键而绕过闸门（BC-21）
- 最终评级块只验标记存在，模型保留标记即可改写等级（BC-22）
- Critic 扫描器看最终报告、LLM 却看章节草稿（BC-23）
- `--ablate scanner` 在 `_review_content` 关闭后，被 `merge_review` 再次执行（BC-24）
- 模型 API 失败被完整组的扫描器兜底记成成功、关闭组记成漏检（BC-25）
- 冲突扫描器误拦规范披露，同时漏掉“到位资本”同义改写（BC-26）

同时把 BC-19 从“整份档案非空”升级为字段级一致性重放校验。
当前确定性验证：清单 15/15、评分卡 14/14、主流程 20/20、Critic 门控 23/23、
评测装置 3/3，合计 **75/75**；快速层字段状态 86/86、扫描器显式违规 7/7、对照误报 0/18。
v3 四组开发集与首次留出集均已完成并保存原始 JSONL。结构化清单的增益得到复现。
第二套独立盲测也已完成：完整架构存在 2 个稳定目标漏检、4 个稳定误报，
**v0.5 功能实现完成，但质量未封板**。盲测暴露扫描器否定作用域误判（BC-28）和
LLM 严重度/裁决不一致（BC-29）。该盲测现已退役为回归集；详见
`backend/eval/BLIND_V2_SEAL_REPORT.md`。修复后二次封板必须使用新的未见盲测集。

---

## 迭代 v0.6a：核实来源与结构化证据链（2026-08-11，经一轮只读复核返工）

**动机**：v0.5 交叉复核发现 `verified_profile_mismatches()` 有结构性缺陷——
它假设所有 verified 都能由初始档案重放。接入结构化适配器那天，
新查到的合法证据会被判成状态漂移 → 全面 fail-closed。
**失败方向与系统目标相反：数据源越多，评级越不可用**（BC-30）。

**做了**
- 新增 `service/verification.py`：来源闭集、受信任适配器注册表、结构化证据、
  按来源分发重放、评分数据视图
- `FieldCheck` 增溯源字段：`verification_origin` / `evidence_ids` /
  `source_adapter` / `retrieved_at`
- `ResearchState` 增 `evidence_store`
- `fill_field_checks()` 填充后自动打 `initial_profile` 来源戳，
  取证时间取自档案声明
- 新增 `config/verification_policy.py`：授信口径开关集中可见
- DataAnalyst 改用 `verify_field_checks()` + `build_scoring_view()`，
  降级经 `apply_provenance_gate()` 约束等级

**四条锁死的规则**
1. `attempted_sources` 不是证据——只说明尝试过
2. 通用网页检索不是合法来源——来源与适配器**都是闭集**，集合外 fail-closed
3. 来源不明不得静默猜测——且不得继续形成自动授信等级
4. 通过校验 ≠ 进入评分——证据必须能并进评分卡真正读的那份数据

**明确未做**（本轮边界）
- 未实现任何真实外部适配器（注册表为空，只有测试替身登记）
- 未让 Scout 升级字段状态
- evidence_store 未接入检查点持久化与 SSE
- 未做证据时效性策略（校验时间戳存在且合法，未校验过期）

### ⭐ 实际发现：首版 22 例全绿，但三个洞全在

只读复核推翻了本轮的验收结论。三条断言我写在交付说明里，全都不成立：

| 我当时的说法 | 实情 |
|---|---|
| "绑定之后那条路径在代码层走不通" | 函数**根本没改状态**，绑定只写在注释里（BC-34） |
| "闭集之外一律 fail-closed" | 写入口接受任意 `source_adapter` 字符串，`web_search` 可直接获得结构化身份（BC-32） |
| "22 例覆盖全部 10 项必测场景" | 覆盖了，但全部停在 `verify_field_checks().ok`，没有一条走到最终评级 |

**最严重的是第三条派生出的 BC-31**：适配器写入「存在1800万元担保」，
证据链校验通过，评分卡仍输出「未发现对外担保」——
**新增的负面证据被翻译成了有利于放款的结论**。

复核共提出 3 个 P0、2 个 P1，逐条构造复现后**全部属实**，已全部修复：

| 编号 | 缺陷 | 同形前案 |
|---|---|---|
| BC-31 | 证据通过校验却没进评分数据 | BC-19（换了个入口） |
| BC-32 | 证据不绑定字段/适配器/时间/冲突状态 | — |
| BC-33 | 来源降级只披露、不约束等级 | BC-17（机制存在但无约束力） |
| BC-34 | 「写证据与改状态绑定」只存在于注释 | BC-15（跑通了但测的不是那件事） |
| BC-35 | 用运行时间冒充取证时间 | BC-21（遍历"已有数据"，最缺的那项漏掉） |

**这一轮真正的收获不是证据链本身，而是一条测试方法论：**

> 断言停在中间报告上，等于没有断言。
> **中间报告为真，不代表最终决定为真。**

首版的 22 条断言全绿且逻辑正确，唯一的问题是它们都不到终点。
现在核心用例一律断言到 `DataAnalyst.assess_risk()` 与 `research_complete`。

**验收**：`tests/test_verification_chain.py` 44 例全通过；
确定性回归 119 项断言 + 快速层 + 扫描器全绿。

**返工后尚未再次提交 Critic 只读复审，不得宣布通过。**

---

## 迭代 v0.6：人机协同 → 倒逼修复 LangGraph

**动机**：v0.5 有了风险等级后，业务上高风险结论**不能全自动放行**，必须人工确认（合规要求，也是出坏账追责的前提）。

**这一轮的技术转折点**：实现"暂停 → 等人工确认 → 继续"最干净的方式是 LangGraph 原生的 `interrupt`。而**当前 LangGraph 执行路径是被注释掉的死代码**（`graph.py:349-356`）——业务需求倒逼了这次架构修复。

> 这个因果链是真实且有说服力的：不是"我看到死代码手痒重构"，而是"业务要求人在回路，迫使我恢复真正的图执行"。

**范围内**
- 用 `get_stream_writer()` + `stream_mode="custom"` 恢复图执行**同时保留实时 SSE**——这正是原作者当年放弃 LangGraph 的原因，在 langgraph 1.2.x 已有解法（迭代 0 已实测验证 API 可用）
- 删除 `_run_simplified` 中重复的命令式控制流，消除双份实现
- `interrupt` 实现复核卡点；`checkpoint_service` 已支持 `paused` 状态但从无代码设置它，正好接上

**预期问题**
- 恢复图执行后事件顺序/时序可能变化，前端需适配
- interrupt 与项目自有的 checkpoint 机制职责需划清

**验收**：高风险结论触发暂停，前端出现确认卡片，确认后从断点继续；SSE 实时性不退化。

---

## 迭代 v0.7：性能与成本

**动机**：迭代 0 实测 834 秒/次。业务约束是"业务员在等着回客户"，10 分钟是上限，当前已经贴线。

**范围内**
- Scout 多源检索并行化（当前疑似串行，需先确认）
- 模型路由：字段抽取用便宜模型，风险推理用强模型（`llm_config.py` 已有 per-agent 配置，是现成抓手）
- 全链路 token/耗时/成本埋点，**优化前后对比数据写进 README**
- 失败降级：工具失败、JSON 解析失败、模型拒答

**预期问题**：并行后触发 API 限流；便宜模型在字段抽取上准确率下降——**此时 v0.4 的评测就是安全网**，能直接量化"省了多少钱、掉了多少分"。

**验收**：耗时与成本显著下降，且评测四项指标不明显退化。

> 这一轮能成立，完全依赖 v0.4 建立的评测能力。**没有评测就不敢优化**——这也是"评测必须早做"的实证。

---

## 迭代 v1.0：交付形态

**动机**：功能可用 ≠ 能交付。业务侧还缺"没人愿意做但必须有"的部分。

**范围内**
- Word 导出（提交信贷评审会）
- `AuditLog` 操作留痕（事后追责）
- 前端：企业档案页、尽调流程页、核实率进度条、未核实清单、担保圈图谱、事实溯源交互
- 版权头替换 + LICENSE + 依赖清理（合并两份冲突的 requirements.txt）
- README（面向面试官）+ `ARCHITECTURE.md`
- 演示 GIF / 在线 Demo

---

## 与旧计划的映射

原 `MIGRATION_PLAN.md` 按模块分 Stage，仍是**文件级改动清单**的权威来源；执行顺序以本文档为准。主要调整：

| 调整 | 原因 |
|---|---|
| 数据源适配层 Stage 1 → **v0.4** | 真正动机是"评测需要 ground truth"，而非"没有数据" |
| 评测 Stage 5 → **v0.4** | 不能度量就不能改进；后续所有优化都依赖它 |
| 风险评分卡 Stage 2.3 → **v0.5** | 动机应来自评测暴露的真实失效，而非凭空设计 |
| LangGraph 修复 Stage 0.5 → **v0.6** | 由人机协同的业务需求倒逼，因果链更真实；且改动集中在 graph.py 一个文件，晚做不增加返工 |
| 新增 **v0.1 粗糙 Demo** | 先证伪再投入，避免在错误方向上建复杂结构 |

---

## 面试问答映射

| 常见问题 | 答案出处 |
|---|---|
| 讲讲这个项目怎么做出来的 | 演进主线七个箭头 |
| 遇到最大的困难 | v0.3→v0.4：改了但不知道有没有用，被迫先建评测 |
| 怎么保证大模型不胡说 | v0.2→v0.3：从提示词约束升级为架构约束 |
| 做过什么权衡 | v0.7：便宜模型省成本 vs 准确率下降，用评测量化决策 |
| 重构经历 | v0.6：业务需求倒逼恢复被绕过的编排框架 |
| 怎么衡量效果 | v0.4 四项指标 + v0.7 优化前后对比 |
| **遇到过什么 badcase** | [`BADCASES.md`](BADCASES.md)，7 个已实测 + 复盘的五类共性模式 |
| **怎么排查问题** | `BADCASES.md` 每条的「定位过程」——面试真正深挖的是方法而非结论 |
